#!/bin/bash
# =========================================================
# تشغيل بوت الجافاسكريبت (discord.js)
# =========================================================
cd "$(dirname "$0")"

# تثبيت الاعتماديات إن لم تكن مثبتة
[ -d node_modules ] || npm install

# تشغيل البوت
node bot.js
